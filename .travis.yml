sudo: required
dist: trusty
language: node_js
node_js:
    - '10'

cache:
    yarn: true
    directories:
        - node_modules

before_install:
    - npm install -g yarn lerna

install:
    - lerna bootstrap
    - yarn build
    - yarn lint:simulator

script: yarn test
