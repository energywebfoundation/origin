{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Compile",
            "runOptions": {
                "runOn": "default"
            },
            "command": "yarn run build",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
