import { Connect } from './connect/connect.entity';
import { Registration } from './registration/registration.entity';

export * from './app.module';

export const entities = [Connect, Registration];
