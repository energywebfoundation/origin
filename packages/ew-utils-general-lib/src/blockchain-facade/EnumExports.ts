export enum TimeFrame {
  yearly,
  monthly,
  daily,
  hourly
}

export enum Currency {
  Euro,
  USD,
  SingaporeDollar,
  Ether
}

export enum AssetType {
  Wind,
  Solar,
  RunRiverHydro,
  BiomassGas
}

export enum Compliance {
  none,
  IREC,
  EEC,
  TIGR
}
