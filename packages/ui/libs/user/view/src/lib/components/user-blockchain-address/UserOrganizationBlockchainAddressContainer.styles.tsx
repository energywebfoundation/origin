import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  infoIcon: {
    marginLeft: '1rem',
  },

  buttonAndIconHolder: {
    paddingTop: '1rem',
    display: 'flex',
    alignItems: 'center',
  },
});
