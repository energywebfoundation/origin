import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  blockWrapper: {
    display: 'flex',
    alignItems: 'center',
  },
  dotWrapper: {
    display: 'inline-block',
  },
});
