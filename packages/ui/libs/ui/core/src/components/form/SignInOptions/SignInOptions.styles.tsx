import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  checkbox: {
    '&.MuiCheckbox-root': {
      paddingLeft: 0,
    },
  },
  forgotPassword: {
    textDecoration: 'none',
  },
});
