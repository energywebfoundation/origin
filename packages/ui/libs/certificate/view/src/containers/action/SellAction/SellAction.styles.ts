import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  totalPrice: {
    margin: '20px 0',
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
  },
});
