import React from 'react';
import { UiCore, UiCoreProps } from './ui-core';

export default {
  component: UiCore,
  title: 'UiCore',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props: UiCoreProps = {};

  return <UiCore />;
};
