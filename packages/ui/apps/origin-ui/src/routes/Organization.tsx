import React from 'react';
import {
  OrganizationApp,
  OrganizationAppProps,
} from '@energyweb/origin-ui-organization-view';

const OrganizationRoute = (props: OrganizationAppProps) => {
  return <OrganizationApp {...props} />;
};

export default OrganizationRoute;
