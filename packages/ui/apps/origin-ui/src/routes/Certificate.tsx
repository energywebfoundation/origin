import React from 'react';
import {
  CertificateApp,
  CertificateAppProps,
} from '@energyweb/origin-ui-certificate-view';

const CertificateRoute = (props: CertificateAppProps) => {
  return <CertificateApp {...props} />;
};

export default CertificateRoute;
