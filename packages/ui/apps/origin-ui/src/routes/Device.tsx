import React from 'react';
import { DeviceApp, DeviceAppProps } from '@energyweb/origin-ui-device-view';

const DeviceRoute = (props: DeviceAppProps) => {
  return <DeviceApp {...props} />;
};

export default DeviceRoute;
