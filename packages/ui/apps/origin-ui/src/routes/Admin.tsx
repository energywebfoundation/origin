import React from 'react';
import { AdminApp, AdminAppProps } from '@energyweb/origin-ui-user-view';

const AdminRoute = (props: AdminAppProps) => {
  return <AdminApp {...props} />;
};

export default AdminRoute;
