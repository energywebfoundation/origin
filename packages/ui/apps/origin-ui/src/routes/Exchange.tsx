import React from 'react';
import {
  ExchangeApp,
  ExchangeAppProps,
} from '@energyweb/origin-ui-exchange-view';

const ExchangeRoute = (props: ExchangeAppProps) => {
  return <ExchangeApp {...props} />;
};

export default ExchangeRoute;
