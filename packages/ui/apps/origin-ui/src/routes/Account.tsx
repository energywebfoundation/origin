import React from 'react';
import { AccountApp, AccountAppProps } from '@energyweb/origin-ui-user-view';

const AccountRoute = (props: AccountAppProps) => {
  return <AccountApp {...props} />;
};

export default AccountRoute;
