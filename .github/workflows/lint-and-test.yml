name: Lint & test

on:
  pull_request:
    branches:
      - '*'

jobs:
  cancel-previous:
    name: 'Cancel Previous Runs'
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}

  run-tests:
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: 'origin'
          POSTGRES_PASSWORD: 'postgres'
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 10s
          --health-retries 10
        ports:
          - 5432:5432
    runs-on: ubuntu-latest
    needs: cancel-previous
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - uses: actions/setup-node@v2-beta
      with:
        node-version: '14'

    - name: Restore timestamp
      run: |
        sh scripts/restore-original-timestamps.sh

    - name: Check dependency versions
      run: node common/scripts/install-run-rush.js check

    - name: Install application
      run: |
        node common/scripts/install-run-rush.js update

    - name: Build application
      run: node common/scripts/install-run-rush.js rebuild --verbose

    - name: Run all test:ui:e2e
      env:
        WEB3: http://localhost:8553
        DEPLOY_KEY: d9066ff9f753a1898709b568119055660a77d9aae4d7a4ad677b8fb3d2a571e5
        INFLUXDB_URL: ${{secrets.INFLUXDB_URL}}
        INFLUXDB_TOKEN: ${{secrets.INFLUXDB_TOKEN}}
        INFLUXDB_BUCKET: ${{secrets.BUCKET}}
        BACKEND_URL: ${{secrets.BACKEND_URL}}
        EXCHANGE_WALLET_PUB: ${{secrets.EXCHANGE_WALLET_PUB}}
        EXCHANGE_WALLET_PRIV: ${{secrets.EXCHANGE_WALLET_PRIV}}
        EXCHANGE_ACCOUNT_DEPLOYER_PRIV: ${{secrets.EXCHANGE_ACCOUNT_DEPLOYER_PRIV}}
      run: node common/scripts/install-run-rush.js test:ui:e2e