version: '3'
services:
  origin_ui:
    image: "energyweb/origin-ui:${TAG}"
    ports: 
      - "80:80"
    networks:
      - app-network
  market_matcher:
    image: "energyweb/market-matcher:${TAG}"

networks:
  app-network:
    driver: bridge