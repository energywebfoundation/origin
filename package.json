{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/chai": "^4.2.0",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.7.2",
    "chai": "^4.2.0",
    "lerna": "^3.16.4",
    "mocha": "^6.2.0",
    "prettier": "^1.18.2",
    "tslint": "^5.19.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-lines-between-class-members": "^1.3.6",
    "tslint-sonarts": "^1.9.0",
    "typescript": "^3.5.3",
    "typescript-json-schema": "^0.39.0"
  },
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint --parallel --stream",
    "run:ui": "lerna run start --scope ew-origin-ui --stream",
    "test": "yarn run test:ui",
    "test:ui": "lerna run test --scope ew-origin-ui --stream"
  },
  "workspaces": [
    "packages/*"
  ]
}
